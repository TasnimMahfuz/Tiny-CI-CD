{
  "name": "tiny-ci-cd",
  "version": "1.0.0",
  "scripts": {
    "start": "uvicorn main:app --host 0.0.0.0 --port 8080",
    "vercel-build": "pip install -r requirements.txt"
  },
  "dependencies": {
    "vercel": "^39.3.0"
  }
}
